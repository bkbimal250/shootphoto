# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
# For local development:
# MONGODB_URI=mongodb://localhost:27017/shootic
# For MongoDB Atlas (production):
MONGODB_URI=mongodb+srv://shootic:6cqA7sx6QgZxpwE7@cluster0.0g9epcq.mongodb.net/Shootic-photo

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=24h

# Email Configuration (Nodemailer)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=info.shootic@gmail.com
EMAIL_PASS=cfwm duyq yjnf cjuz
EMAIL_FROM=Shootic Photography <info.shootic@gmail.com>

# For Gmail, you'll need to:
# 1. Enable 2-factor authentication
# 2. Generate an App Password
# 3. Use the App Password instead of your regular password

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
RATE_LIMIT_MAX_REQUESTS=100  # 100 requests per window

# CORS Configuration
CORS_ORIGIN=http://localhost:3000
# For production, add your frontend domain:
# CORS_ORIGIN=https://yourdomain.com

# Security
HELMET_ENABLED=true
COMPRESSION_ENABLED=true

# Logging
LOG_LEVEL=info
# Options: error, warn, info, http, verbose, debug, silly

# Optional: External Services
# If you want to use external services like Resend for emails
# RESEND_API_KEY=your-resend-api-key

# Optional: File Upload (if you plan to add image uploads)
# UPLOAD_PATH=./uploads
# MAX_FILE_SIZE=5242880  # 5MB in bytes
